# Reproducible reference environment (CPU wheels by default).
# CUDA users should install the matching PyTorch wheel from pytorch.org for
# their CUDA runtime, then keep the same torch major/minor where possible.
torch==2.3.1
numpy==1.26.4
pytest==8.2.2
tensorboard==2.17.0
requests==2.32.3
