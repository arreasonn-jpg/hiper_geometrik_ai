# ── Çekirdek bağımlılık ──────────────────────────────────────────────────────
# Esnek geliştirme kurulumu. Tekrarlanabilir referans için requirements-lock.txt kullanın.
# torch>=2.0.0 (SDPA/Flash Attention ve use_reentrant=False checkpointing için)
torch>=2.0.0,<3

# PyTorch bazı başlatma/değerlendirme yollarında NumPy köprüsünü yoklar;
# numpy yoksa testleri bozmasa da uyarı üretir. 1.x aralığı torch 2.3 ile güvenlidir.
numpy>=1.26,<2

# ── Opsiyonel: test ve logging ───────────────────────────────────────────────
# pytest>=8.0
# tensorboard>=2.16

# ── Opsiyonel: arayuz.py (Gradio sohbet arayüzü) ─────────────────────────────
# gradio>=4.0

# ── Opsiyonel: egitim/veri_toplayici.py (korpus toplama) ─────────────────────
# requests>=2.28
# pyarrow>=14.0
# pandas>=2.0
